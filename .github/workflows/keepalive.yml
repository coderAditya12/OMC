name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/13 * * * *" 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl -s https://opensource-compass.onrender.com/ > /dev/null
